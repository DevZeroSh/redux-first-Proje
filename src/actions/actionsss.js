import { plass, min, Reset } from "../type/type"

export const inc = () => {
    return { type: plass }
}
export const mine = () => {
    return { type: min }
}
export const q = () => {
    return { type: Reset }
}